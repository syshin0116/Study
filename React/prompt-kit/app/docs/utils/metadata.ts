export function generateMetadata(title: string, description: string) {
  return {
    title: `${title} - prompt-kit`,
    description: `${description} Built with React, shadcn/ui and Tailwind CSS, part of prompt-kit, a library of customizable components for AI apps.`,
  }
}
