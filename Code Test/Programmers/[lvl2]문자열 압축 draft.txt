def solution(s):
    print(len(s))
    answer_list = []
    compress_length = []
    for compress_length in range (1, int(len(s)/2)):
        answer_dict = {}
        for i in range(len(s)):
            if s[i:i+compress_length] == s[i+compress_length:i+2*compress_length]:
                answer_dict[s[i:i+compress_length]] = s.count(s[i:i+compress_length])
        print(compress_length, ">>", answer_dict, len(s)-(compress_length*sum(answer_dict.values()))+compress_length*len(answer_dict))
        answer_list.append(answer_dict)
        
    print(answer_list)
    answer = 0
    return answer